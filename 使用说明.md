# 贵小溪学习平台 · 自动刷课助手（使用说明）

一个 Tampermonkey（油猴）用户脚本：按 1x 正常速度自动挂机看完所有课程视频；每章看完后自动返回课程列表、按「学习进度」进入下一章节；在线测试接入 DeepSeek API 自动答题并交卷。

## 为什么默认 1x 挂机

经实测，该平台有服务端校验：倍速播放或直接跳到结尾，学习进度都不会被记录。所以脚本默认使用「🐢挂机1x」模式——老老实实正常速度播放，挂机时长约等于剩余课程总时长。看完后脚本会等待进度上报、再自动进入下一课，全程无需人工干预。

## 功能一览

### 挂机刷课（原有功能）
- 🐢 挂机1x（默认）：正常速度自动播放、防暂停、默认静音；
- 看完后自动流程：等待约 3.5 秒进度上报 → 自动刷新页面 → 自动点「下一讲」或从课程目录点击下一课；
- 支持折叠章节：下一课在折叠章节里时自动展开；跨章节后自动折叠上一章节；
- 若下一课未解锁：自动从头重播当前课程并反复重试；
- ⚡加速 / ⏭跳结尾：保留为实验选项（服务端校验下大概率不记录进度，仅作测试用）。

### 🆕 章节切换（按学习进度）
- 当一个章节的最后一课看完后，脚本会：等待进度上报 → 返回课程列表页面 → 读取每章下方显示的「学习进度」→ 跳过已经 100% 的章节 → 点击进入下一个未完成的章节（例如刚看完第一章就进第二章）；
- 进入章节后如落在视频列表页，会自动点开第一个视频继续挂机；
- 全部章节进度都到 100% 时，日志提示课程已学完并停止；
- 面板提供「📑 章节切换:开/关」开关和「⬅ 回列表换章」手动按钮；
- 返回课程列表优先用 history.back()；若失败（比如新标签页打开的视频）会自动尝试点击页面上的「返回课程」链接，再不行则退回原来的“目录点下一课”流程。

### 🆕 在线测试自动答题（DeepSeek）
- 点面板「⚙ 答题设置」，填入 DeepSeek API Key（sk-...）并「💾 保存」，自动答题即开启（默认接口 https://api.deepseek.com/v1，模型 deepseek-chat，都可改）；
- 从课程列表点入「在线测试」进入答题页后，脚本自动检测题目：直接读取页面上的题目文字与选项（**无需截图 OCR**，比截图识别更准确），把题目发给 DeepSeek，由模型返回每题的正确答案（单选/多选自动区分）；
- 自动点选正确答案 → 全部答完后自动点击「交卷」，并自动点击确认交卷弹窗；
- 分页试卷（下一题/下一页）自动翻页继续作答，直到交卷；
- 面板按钮：「🤖 自动答题」开关、「▶ 手动答题」随时触发、「📋 交卷」手动交卷、「✅ 自动交卷」开关（关闭后答完不交卷，方便先检查再手动交）、「📸 截图」可选保存试卷截图（仅作记录，不影响答题）。

## 安装步骤

1. 给浏览器安装 Tampermonkey 扩展（Edge/Chrome 扩展商店搜「Tampermonkey」，国内浏览器一般叫「油猴/暴力猴」也通用）；
2. 点击 Tampermonkey 图标 → 「管理面板」→「已安装脚本」旁的 ➕ 新建脚本；
3. 删掉模板内容，把 gxx-auto-watch.user.js 文件里的全部代码粘贴进去，Ctrl+S 保存；
4. 打开任一课程视频页面（需已登录），右上角出现「🎓 贵小溪刷课助手」面板即生效。

## 使用建议

1. 默认就是挂机1x，不用动任何设置，打开第一课页面放着即可；章节会自动切换，课程会自动看完；
2. 要自动答题时，先在面板「⚙ 答题设置」里保存 DeepSeek API Key（Key 只存在本机浏览器 localStorage，不上传别处）；
3. 挂机前把系统电源设置为「从不睡眠」（Windows：设置 → 系统 → 电源）；浏览器里给该网站关掉标签页休眠（Edge：设置 → 系统和性能 → 「从不将这些站点置于睡眠状态」→ 添加 gxx-edu.digitlanguage.com）；
4. 可以最小化窗口，但不要关闭标签页；
5. 建议睡前挂上，第二天早上检查；中途断网/休眠也没关系，重新打开页面会自动续播。

## 常见问题

- 面板不出现：确认脚本已启用、网址以 gxx-edu.digitlanguage.com 开头、已登录平台，然后刷新页面；
- 状态栏显示「未发现视频元素」但视频在播：播放器在跨域 iframe 里，右键视频区域选「查看框架源代码」把 iframe 网址发我，我再适配；
- 章节切换不动：确认面板「📑 章节切换」是开；脚本靠页面上的「第X章」文字和「学习进度(百分比/已学完/进度条)」来判定，如果你的课程列表页没有这些文字，请把页面 HTML 结构发我适配；
- 答题时日志提示「网络请求被拦截(CORS/CSP)」：按脚本文件头部注释，把 @grant none 换成 @grant GM_xmlhttpRequest + @connect api.deepseek.com（如使用其他 API 地址，把 @connect 改成对应域名），保存后刷新页面即可；
- 检测到测试页面但未识别到题目：题目可能在跨域 iframe 里，或者页面用了特殊组件，把手动作答页的 HTML 结构发我适配；
- 图片型题目（题干/选项是图片）无法自动作答：模型读不到文字，只能手动；
- 自动交卷怕出错：把面板「✅ 自动交卷」关掉，脚本答完会停下，检查后再手动点「📋 交卷」；
- 加速/跳结尾模式下进度没记录：属正常现象（服务端校验），切回挂机1x即可。

## 免责声明

本脚本仅供个人账号加快完成自有课程学习使用，请遵守平台相关规定，勿用于违规用途。自动答题功能请仅用于自己需要完成的测验。
